node_modules/
dist/
.astro/
.DS_Store
**/.DS_Store
.env
.env.*
!.env.example

# Photo app lock/sidecar files
*~lock~
*.af~lock~
*.xmp

# Tooling artifacts at project root
/*.png
.playwright-mcp/
.screenshots/
.vercel

# Generated by scripts/write-monthly-redirect.mjs during prebuild — always
# reflects the latest monthly entry, no need to track history.
/vercel.json

# Originals — local only; processed copies go to src/content/
originals/

# Audio source files — keep originals out of repo, compressed copy lives in public/audio/
Music/
